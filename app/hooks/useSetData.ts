// import { ref, set } from 'firebase/database';
// import dbFirebase from '@/lib/firebase';

// export const useSetData = () => {
//   const setData = async (path: string, data: any) => {
//     try {
//       await set(ref(dbFirebase, path), data);
//     } catch (error) {
//       console.error(error);
//     }
//   };

//   return { setData };
// };
